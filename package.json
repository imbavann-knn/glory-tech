{
  "name": "glory-tech",
  "version": "1.0.0",
  "private": true,
  "description": "Glory Tech static website",
  "scripts": {
    "start": "serve . --listen tcp://0.0.0.0:${PORT:-3000} --no-clipboard"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
